# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/reference/pip_install/#caching
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip
    - hypervenv/

before_script:
  - apt-get update && apt-get install --assume-yes python3 python3-pip python3-venv
  - python3 --version
  - python3 -m venv hypervenv --without-pip --system-site-packages
  - source hypervenv/bin/activate

install_latest:
  image: python:latest
  script:
    - python setup.py check
    - pip install .
#    - pip install tox flake8 
#    - tox -e py36,flake8


install_slim:
  image: python:slim
  script:
    - python setup.py check
    - pip install .

#install_stretch:
#  image: git.physik.uni-wuerzburg.de:25812/z03/pdi/debian:stretch-gfortran-blas-lapack-fftw-hdf5-scipy3
#  script:
#    - python3 setup.py check
#    - pip3 install .

#install_buster:
# image: git.physik.uni-wuerzburg.de:25812/z03/pdi/debian:buster-gfortran-blas-lapack-fftw-hdf5-scipy3
 #script:
 #  - pip3 install .

install_bullseye:
 image: git.physik.uni-wuerzburg.de:25812/z03/pdi/debian:bullseye-gfortran-blas-lapack-fftw-hdf5-scipy3
 script:
   - pip3 install .



unit-test:
    stage: test
    image: git.physik.uni-wuerzburg.de:25812/z03/pdi/debian:bullseye-gfortran-blas-lapack-fftw-hdf5-scipy3
    script:
        - pip install .
        - echo "Testing ..."
        - python3 -m tests.test_core 

#Sphinx_doc:
#  image: git.physik.uni-wuerzburg.de:25812/z03/pdi/debian:bullseye-gfortran-blas-lapack-fftw-hdf5-scipy3
#  stage: build
#  artifacts:
#    paths:
#      - doc/*
#  script:
#    - apt-get update && apt-get install -y python3-numpy python3-colorama python3-h5py doxygen python3-breathe git python3-sphinx-rtd-theme
#    - cd doc
#    - sphinx-build . html
#
